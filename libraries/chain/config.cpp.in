#include <eosio/chain/config.hpp>

namespace eosio::chain::config {

   name system_account_name      = { "${SYSTEM_ACCOUNT_NAME}"_n };
   name null_account_name        = { "${SYSTEM_ACCOUNT_PREFIX}.null"_n };
   name producers_account_name   = { "${SYSTEM_ACCOUNT_PREFIX}.prods"_n };
   name eosio_auth_scope         = { "${SYSTEM_ACCOUNT_PREFIX}.auth"_n };
   name eosio_all_scope          = { "${SYSTEM_ACCOUNT_PREFIX}.all"_n };
   name eosio_any_name           = { "${SYSTEM_ACCOUNT_PREFIX}.any"_n };
   name eosio_code_name          = { "${SYSTEM_ACCOUNT_PREFIX}.code"_n };
   name token_account_name       = { "${SYSTEM_ACCOUNT_PREFIX}.token"_n };
   name msig_account_name        = { "${SYSTEM_ACCOUNT_PREFIX}.msig"_n };
   name wrap_account_name        = { "${SYSTEM_ACCOUNT_PREFIX}.wrap"_n };
   name gas_account_name         = { "${SYSTEM_ACCOUNT_PREFIX}.gas"_n };

   symbol core_symbol            = symbol(${CORE_SYMBOL_PRECISION}, "${CORE_SYMBOL_NAME}");
   symbol_code core_symbol_code  = core_symbol.to_symbol_code();

} /// namespace eosio::chain::config
